/**
 * Шаблон для компонента "{{ name }}".
 */
export default {

    render( props ) {
        return `${this.css( props )}`;
    },

    /**
     * Кэширование элементов компонента для теневой модели.
     */
    mapDom( scope ) {
        return {
            oneDiv: scope.querySelector('.ddt'),// Для примера.
        }
    },

    /**
     * Перемещает стили в компонент.
     */
    css( p ) { return `
        <style>
            :host { color: red; }// Для {{ name }}.
        </style>`;
    },
}

